# Normalize line endings to LF in the repo (avoids CRLF churn on Windows).
* text=auto eol=lf

# Binary / generated: never diff or convert.
pnpm-lock.yaml -diff
*.png binary
*.jpg binary
*.ico binary
